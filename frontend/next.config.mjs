/** @type {import('next').NextConfig} */
// Note: NEXT_PUBLIC_API_URL is inlined at build time — changing it requires a
// fresh build (rebuild), not just a restart/redeploy of the existing image.
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
